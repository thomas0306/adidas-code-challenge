// import { selectWishlistPageDomain } from '../selectors';

describe('selectWishlistPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
