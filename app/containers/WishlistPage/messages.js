/*
 * WishlistPage Messages
 *
 * This contains all the text for the WishlistPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WishlistPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WishlistPage container!',
  },
});
