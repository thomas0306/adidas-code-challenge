/*
 * SuggestionList Messages
 *
 * This contains all the text for the SuggestionList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SuggestionList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SuggestionList component!',
  },
});
